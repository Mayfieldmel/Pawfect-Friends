<article class="post">
  <div class="title">
    <h3>{{title}}</h3>
  </div>
  <div class="meta">
    {{log post}}
    by {{Pet.pet_name}} on {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>